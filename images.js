
Once square is clicked, identify which column it belongs to
assign class name to each column
each square needs its own identifier along with row class name
each square needs an event listener for clicks


document.querySelector(".circle").addEventListener("click", function(){
  console.log("hello");
})
